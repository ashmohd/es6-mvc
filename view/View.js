class View {
    constructor() {
        this.document = document;

    }
    // bindOnLoad(handler){
    //     this.document.addEventListener(handler())
    // }

    resetForm(form){
        form.reset();
    }
}
export default View;
